services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: speaking_club
      POSTGRES_PASSWORD: speaking_club_dev
      POSTGRES_DB: speaking_club
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
